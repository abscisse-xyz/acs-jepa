# Stage 0C Strict Vertical RED/GREEN Record

Date: 2026-07-22
Authorization: accepted Updated Phase 0 plan §§3,6,8,9; plan review PASS `deleg_7fa43795`.
Scope: implementation and temporary-root verification only. No official evidence, Stage 0D, commit, or push.

## Slice log

This file is updated immediately after each observed behavioral RED/GREEN cycle. Bootstrap/import failures are not counted as behavioral RED evidence.

### Slice 1 — manifest-order hard-negative filtering

- RED command: `UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_transition_equivalence.py::test_filter_hard_negatives_preserves_manifest_order_and_rejects_duplicate_actions -q`
- RED exit: `1`.
- Exact failure: `AttributeError: module 'transition_filter' has no attribute 'filter_hard_negatives'` at the behavioral call after successful module import.
- Minimal implementation: ordered label/schema/category filtering plus duplicate eligible action-key refusal.
- GREEN: the same focused command exited `0` with `1 passed`.

### Slice 2 — raw action-latent nearest hard negative

- RED command: `UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_transition_equivalence.py::test_nearest_wrong_uses_all_64_cpu_float64_coordinates_zero_policy_and_canonical_ties -q`
- RED exit: `1`.
- Exact failure: `AttributeError: module 'transition_distance' has no attribute 'select_nearest_wrong'` after successful import and fixture construction.
- Minimal implementation: detached CPU-float64 64-coordinate copies, finite checks, strict zero-vector unit policy, exact L2, and `(distance, canonical action key)` selection.
- GREEN: the focused distance command exited `0` with `1 passed`.

### Slice 3 — aligned CPU-float64 transition components

- RED command: `UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_transition_equivalence.py::test_transition_components_are_cpu_float64_weighted_and_metadata_aligned -q`
- RED exit: `1`.
- Exact failure: `AttributeError: module 'transition_components' has no attribute 'transition_components'` after the distance slice passed.
- Test correction before GREEN: the initial 2-coordinate fixture contradicted the required 64-coordinate state shape and one value-only row permutation was not detectable from unchanged metadata; the corrected oracle pads to 64 coordinates and mutation reorders/duplicates authoritative IDs. This correction is not claimed as RED.
- Minimal implementation: exact latent shapes/finite values, detached CPU-long canonical IDs/all-zero batch, pairwise metadata/shape equality, exact 1.0 weights, and whole-tensor CPU-float64 means.
- GREEN: the corrected focused component command exited `0` with `1 passed`.

### Slice 4 — ratios, inclusive gates, medians, categories, and minimum count

- RED command: `UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_transition_equivalence.py::test_row_metrics_use_float64_epsilon_inclusive_boundaries_and_exact_aggregations -q`
- RED exit: `1`.
- Exact failure: `AttributeError: module 'transition_metrics' has no attribute 'derived_metrics'`.
- Minimal implementation: float64-epsilon denominator and near-zero predicate; inclusive ratio gates; arithmetic distribution median; eligible-only global/category aggregation; null empty category; inclusive 0.50 mostly gate; fewer-than-40 refusal.
- First GREEN attempt exposed a test-only mapping-size assertion defect because `derived_metrics` correctly returned the two Boolean fields in addition to the three numeric fields. The assertion was narrowed to the numeric projection; this failure is not behavioral RED evidence.
- GREEN: corrected focused aggregation command exited `0` with `1 passed`.

### Slice 5 — strict source/trace/successor-index reconciliation

- RED command: `UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_transition_equivalence.py::test_reconcile_transition_enforces_exact_successor_and_trace_without_reading_successor -q`
- RED exit: `1`.
- Exact failure: `AttributeError: module 'transition_reconcile' has no attribute 'reconcile_transitions'`.
- Minimal implementation: one strict corpus trajectory per problem/step, `len(states)==len(actions)+1`, exact trace identity, source at `states[step]`, and stored successor index only (no successor value field).
- GREEN: focused reconciliation command exited `0` with `1 passed`.

### Slice 6 — fixed CLI/binding and end-to-end artifact path

- RED command: `UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_transition_equivalence.py::test_parser_and_binding_pin_every_stage0c_literal -q`
- RED exit: `1`.
- Exact failure: `AttributeError: module 'transition_parser' has no attribute 'build_parser'` after Slice 5 GREEN.
- Implementation: literal parser/settings validation; checkpoint/output/root/input binding; strict restoration/freeze/inference; native source/action/predict/predict then exact successor encode sequence; exact component/detail/summary construction; canonical two-file output.
- GREEN: focused Stage 0C module exited `0` with `6 passed`.

### Slice 7 — recursive detail schema and exact repeat projection

- RED command: `UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_transition_equivalence.py::test_recursive_artifact_schema_and_repeat_projection_reject_mutations -q`
- RED exit: `1`.
- Exact failure: `AttributeError: module 'transition_schema' has no attribute 'NULLABLE_DETAIL_KEYS'`.
- Implementation: exact top-level/detail/action key sets, eligible/skipped nullability, recursive finite-number validation, and six-pointer summary projection only. Validation is wired before artifact writes.
- The first full focused run exposed realistic manifest encounter order (`p166:0,p166:1,p166:10,...`) differing from the preregistered literal numeric group order. This was a genuine integration RED at the exact-order assertion. The reconciliation adapter now consumes the validated fixed group set in literal `GROUPS` order while preserving candidate order inside each group.
- GREEN after the ordering fix: focused suite exited `0` with `9 passed`.
- Immediate-pass regression/mutation coverage (not claimed as behavioral RED) additionally pins native predictor dtype/device, source/action/predict/predict/successor-target ordering, `include_static=True`, successor isolation, actual 44-group/44-eligible corpus reconciliation, no prohibited imports/calls, fixed bindings, object drop/duplicate/reorder/batch mutations, and schema/projection mutations.

## Final GREEN and non-official verification

- Focused Stage 0C after Ruff formatting: `10 passed`.
- Adjacent Stage 0A/B/Phase 2G files: `53 passed`.
- Full core: `296 passed`.
- Scoped full CLI (`-k 'not tuning_configs_load_and_keep_required_defaults'`): `170 passed`.
- Ruff on both changed Python files: `All checks passed!`.
- `compileall` on both changed Python files: exit `0`.
- `git diff --check`: exit `0`.
- Fresh temporary-root complete command repeats at `/tmp/acs-jepa-stage0c-final-hUxge7/updated_phase0`: baseline and Phase 2 each produced `44 eligible`, `0 skipped`, exact summary projection equality, and byte-identical `details.json`; both descriptive equivalence rates were `1.0`. This is verification only, not official evidence.
- The official `updated_phase0/transition_equivalence` destination remained absent, Git HEAD remained `098ada7d7a09646a9996fa10128d06a844ce7d32`, and no commit/push was performed.
- Expected dependency deprecation warnings were emitted by PyTorch Geometric, Torch JIT, `pkg_resources`, and MLflow; no test or command failed.

## Pre-evidence audit closure — recursive validation and mutation coverage

- Incoming independent audit verdict: **FAIL**. Official evidence authorization remained withheld while the recursive validators, mutation sensitivity, and listed production-boundary regressions were incomplete.
- Genuine validator RED command: `UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_transition_equivalence.py -q -k 'details_validator_rejects_exact_order_types_ranges_and_recomputed_values or summary_validator_rejects_settings_restoration_counts_rates_and_distributions or phase2_restoration_and_repeat_projection_are_fully_validated'`.
- Validator RED result: exit `1`, **22 failed** for the intended missing contracts: ten malformed details were accepted and `validate_summary` did not exist for eleven summary mutations plus Phase-2 restoration/repeat validation.
- Minimal GREEN: exhaustive strict recursive details and transition-summary validation, exact fixed literals/types/order/identities/restoration maps/counts/rates/distributions, exact detail-to-summary recomputation, and `repeat_projection()` calling full summary validation. `run()` now invokes `validate_summary(summary, details)` before either artifact write.
- Validator GREEN result: the corresponding recursive focused selection exited `0` with **23 passed** after adding the wrong-action-schema case.
- Additional genuine RED/GREEN from the complete temporary-root command: runtime validation rejected `torch.torch_version.TorchVersion` rather than emitting a plain JSON `str`; focused regression exited `1` at the exact type assertion, `str(torch.__version__)` was added, and the same test exited `0` with **1 passed**.
- Controlled source-mutation adequacy (restored after each run): wrong distance policy, reordered candidate categories, and default minimum eligible `39` each exited `1` at its intended focused test. These are mutation results, not behavioral RED claims.
- Added production-boundary regression coverage for independently fixed successor under source mutation; label/category filter mutations; exact baseline/Phase-2 restoration binding; changed root marker and existing/partial destination refusal; CUDA-native metadata copied to CPU-long for validation; coherent object-latent plus authoritative-ID reorder and companion drop/duplicate mutations; and exact repeat sibling bytes.
- A value-only latent permutation with unchanged authoritative IDs is not a coherent row reorder and is observationally indistinguishable from arbitrary latent corruption at this boundary. The regression therefore mutates latent rows together with their authoritative IDs and does not claim impossible detection of unchanged-ID value-only corruption.
- Newly added production-boundary tests that passed immediately are regression/mutation coverage only and are not relabeled as behavioral RED evidence.
- Fresh post-validator temporary-root repeats at `/opt/data/cache/acs-jepa-stage0c-validation-Yx4Ygh/updated_phase0`: baseline and Phase 2 each produced `44 eligible`, `0 skipped`; both exact summary projections and `details.json` bytes matched across `run1`/`run2`. The official destination remained absent.
- Post-change focused Stage 0C: **37 passed**. Adjacent Stage 0A/B/Phase 2G: **53 passed**. Full core: **296 passed**. Scoped CLI: **197 passed**. Ruff initially reported six line-length formatting findings; after mechanical Ruff formatting, Ruff, compileall, `git diff --check`, and the affected focused suite were rerun to final GREEN.
- No official evidence, Stage 0D, commit, or push was performed.

## Official Stage 0C evidence

After parent-process confirmation of `37 passed`, Ruff, `compileall`, `git diff --check`, and absence of the official destination, the four exact §6.4 CUDA commands ran sequentially for baseline/Phase 2 `run1`/`run2`; every command exited `0`. Recursive validation was rerun from the saved JSON against each details/summary pair.

- Baseline: `44` groups, `44` eligible, `0` skipped, `0` near-zero true errors; equivalence rate `1.0`; mostly transition-equivalent `true`; median error margin `-3.271241607371374e-08`; median separation ratio `1.193190858506874e-07`.
- Phase 2: `44` groups, `44` eligible, `0` skipped, `0` near-zero true errors; equivalence rate `1.0`; mostly transition-equivalent `true`; median error margin `-7.279481795032915e-08`; median separation ratio `6.829820330356895e-07`.
- Both checkpoint repeat pairs have exact six-pointer summary projection equality and byte-identical `details.json`.
- Baseline `details.json`: `39,663` bytes, SHA-256 `4c80774626ff5752c84dd6d7b1e8c010cece11fe17f9772cbe8484b40bf85b33` in both repeats.
- Phase 2 `details.json`: `39,531` bytes, SHA-256 `4883f0049a3338dc9cb13e409eaac500c74157923a802605ff1ea4bcdbf6b0a4` in both repeats.
- Baseline summary SHA-256: run1 `670487be555ede346e5c3ed67d95efecf3b8def70282276a5bd022a44127f91f`, run2 `a5a2e8bf3e07a14ebf6e3f766457492f587c12a07a0003aa65fef9e84e371caa`.
- Phase 2 summary SHA-256: run1 `80eac66442e640afc0f948fc74c143262d7a0c1fb303f45691ee963917b1c016`, run2 `78b85b99dfc56fa360ebc2541dcaf5f0d4a45fb2702854b6c097a7e0ae2d9e14`.

These results are diagnostic Stage 0C evidence only. They do not authorize Stage 0D, branch selection, or a commit before independent implementation/evidence review PASS.

## Review 1 rejection and clean test-first reimplementation restart

- Incoming Stage 0C Implementation/Evidence Review 1 verdict: **FAIL**. The verdict was appended to the master review record before source changes. The previously official Stage 0C tree was moved without overwrite to `/opt/data/workspace/acs-jepa-runs/smoke/action_auxiliary_seed0/updated_phase0-stage0c-review1-rejected/transition_equivalence`; the accepted `updated_phase0/transition_equivalence` destination is absent. The rejected source bytes (`fa89c2f...30a51`) were preserved outside the repository at `/opt/data/cache/acs-jepa-stage0c-review1-rejected-source/diagnose_action_transition_equivalence.py`.
- This is an explicit discarded-implementation restart, not a retroactive lifecycle claim or mutation proof. After the complete corrected test suite existed, production was replaced with an importable interface-only skeleton containing constants, the transition dataclass, simulator-free imports, parser interface, and explicit `NotImplementedError` behavior stubs.
- Complete-suite RED: focused Stage 0C exited `1` with **44 failed, 1 passed** after successful imports. Major filtering, distance, alignment, metrics, aggregation, reconciliation, argument binding, recursive validation, environment, causal evaluation, real-corpus reconciliation, and actual CPU causality tests each reached behavior-specific stub calls. A fresh temporary-root real command also imported/parsed successfully and exited `1` at `NotImplementedError: ... complete Stage 0C command is not implemented`, creating no destination.
- Only after that RED, Stage 0C was reimplemented. The corrected validator loads the hash-validated fixed manifest by default, binds unique group/problem/step/trace identities, enforces strict action strings and `list[str]`, requires a unique eligible fixed wrong action/category distinct from trace, validates unit L2 in `[0,2]`, and optionally binds exact reconciled corpus transition identities. `run()` supplies both validated manifest rows and reconciled transitions; `validate_summary()` inherits detail semantics.
- Added semantic adversaries for negative and above-two distances, trace-equal wrong action, absent/noneligible wrong action, corrupted trace/wrong arguments, and summary inheritance. The reconciled-identity contract had its own later behavioral RED (`TypeError: unexpected keyword argument 'reconciled_transitions'`) then GREEN before production wiring.
- The mock action helper now source-conditions native action latents. The actual CPU boundary test uses fixed corpus, baseline bundle, real `build_state_graph`, real action encoder, and real predictor: removing one deterministic non-static atom attached to an argument shared by trace/wrong actions changes source, both native action latents, and both predictions, while the independently encoded recorded successor target remains bit-exact.
- Two fixture corrections during GREEN are not behavioral RED claims: category-diverse valid details replaced an accidental zero-count role-swap no-op mutation, and the actual source mutant was narrowed from an atom affecting only one candidate to a non-static atom attached to an argument shared by trace and wrong actions.
- Final focused Stage 0C: **46 passed**. Adjacent Stage 0A/B/Phase 2G: **69 passed**. Full core: **296 passed**. Scoped CLI: **206 passed**. Ruff, `compileall`, and `git diff --check`: passed.
- Final post-edit temporary repeats at `/opt/data/cache/acs-jepa-stage0c-restart-final-WTPwEH/updated_phase0`: baseline and Phase 2 each produced **44 eligible, 0 skipped**, equivalence rate `1.0`; exact summary projections and `details.json` bytes matched for both repeat pairs. An earlier temporary comparison invocation used system Python without Torch and failed only in the comparison script after all four diagnostic commands had completed; it was rerun under the project environment, then the complete final smoke was rerun under the fresh root above.
- No accepted official Stage 0C evidence was recreated during the delegated correction. No Stage 0D, commit, or push occurred. HEAD remained `098ada7d7a09646a9996fa10128d06a844ce7d32`.

## Corrected official Stage 0C evidence for Review 2

After parent-process confirmation of the 46-test GREEN, static checks, rejected-evidence archive, and absent accepted destination, all four exact §6.4 CUDA commands reran sequentially and exited `0`. The semantic manifest-bound details validator and full summary validator were rerun on every saved pair. Numerical details remained byte-identical to Review 1 because the correction strengthened validation and causality/lifecycle coverage rather than extraction formulas.

- Baseline and Phase 2 each have `44` groups, `44` eligible, `0` skipped, equivalence rate `1.0`, and `mostly_transition_equivalent:true`.
- Both repeat pairs have exact six-pointer summary projection equality and byte-identical `details.json`.
- Baseline details: `39,663` bytes, SHA-256 `4c80774626ff5752c84dd6d7b1e8c010cece11fe17f9772cbe8484b40bf85b33`; corrected summary SHA-256 run1 `60eeb8ca1a7f3d2de4fb388d076cdf1204bba0534b42d45a4916ec2ddd033f09`, run2 `e0b0800daedd6e2fead47d70e9ec3cd1ced32c1d08892e2646f02cdfa543ced7`.
- Phase 2 details: `39,531` bytes, SHA-256 `4883f0049a3338dc9cb13e409eaac500c74157923a802605ff1ea4bcdbf6b0a4`; corrected summary SHA-256 run1 `bbb089690974877d160bb877865b92c005bc6cfa1a5cfc390a022fc92e623cda`, run2 `33814b4992840c7c1271667ba0ed2beef6e1be6cbb067fb9b212c32288741204`.
- Baseline median error margin/separation ratio remain `-3.271241607371374e-08` / `1.193190858506874e-07`; Phase 2 remain `-7.279481795032915e-08` / `6.829820330356895e-07`.

No Stage 0D, branch selection, or commit is authorized before independent Review 2 PASS.

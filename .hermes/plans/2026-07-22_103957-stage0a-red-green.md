# Stage 0A strict RED/GREEN execution log

This log records local software-test commands only. No official evidence command was executed.

## Slice 1 — strict manifest/state/population adapters

### RED

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_phase0_common.py -q`

Observed: **13 failed**. Every test failed with the expected missing-feature error: `FileNotFoundError: script/action_phase0_common.py`.

### GREEN

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_phase0_common.py -q`

Observed: **13 passed** (before the separately tested root protocol was added).

## Slice 2 — float64 centering, variance, residual rank, normalization, nearest tie-break, parser

### RED

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_schema_residuals.py -q`

Observed: **7 failed** with expected missing functions (`schema_residuals`, `raw_variance_decomposition`, `residual_statistics`, `unit_normalize_zero`, `nearest_candidate`) and missing `diagnose_action_schema_residuals.py`.

### GREEN

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_schema_residuals.py -q`

Observed: **7 passed**. A test-only nested `pytest.approx` misuse was corrected to `torch.allclose`; no production behavior changed for that correction.

## Slice 3 — immutable shared-root protocol

### RED

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_phase0_common.py::test_root_protocol_creates_marker_first_then_refuses_changed_or_existing_destinations acs-jepa-cli/tests/test_action_phase0_common.py::test_root_protocol_requires_absent_root_for_first_and_marker_for_later -q`

Observed: **2 failed** with expected `AttributeError: ... has no attribute 'prepare_output_directory'`.

### GREEN

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_phase0_common.py -q`

Observed: **15 passed**.

## Slice 4 — within-state distance details

### RED

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_schema_residuals.py::test_distance_details_use_same_state_only_offline_invalids_and_canonical_ties -q`

Observed: **1 failed** with expected `AttributeError: ... has no attribute 'distance_diagnostics'`.

### GREEN

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_schema_residuals.py -q`

Observed: **8 passed** (three third-party deprecation warnings during diagnostic imports).

## Slice 5 — strict corpus step interpretation found by local population reconciliation

A non-evidence, read-only local reconciliation first failed at `p166:0` because the adapter incorrectly subtracted the database's one-based `start_step_index` from the manifest's trajectory-local zero-based step. A focused fixture was changed to use `start_step_index=7` while retaining manifest `step=0`.

### RED

Command:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_phase0_common.py::test_reconcile_source_states_checks_group_step_and_trace_identity -q`

Observed: **1 failed** with expected `ValueError: group p1:0 does not map to exactly one strict corpus transition`.

### GREEN

Same command after the minimal adapter correction.

Observed: **1 passed**.

Read-only population reconciliation (not an official evidence command) then reported:

`{'groups': 44, 'population': 174780, 'min_bucket': 60, 'max_bucket': 7200, 'manifest_sha256': 'bf6d11149cadf7a34c6c1520e28e9fe389c09c13ce53f3bd3f988f827e936ce9'}`

## Slice 6 — deterministic extraction and strict checkpoint/output binding

### RED

- Determinism test: **1 failed**, expected missing `configure_determinism` attribute.
- Binding test: **1 failed**, expected missing `validate_run_binding` attribute.

### GREEN

Focused command after both minimal implementations:

`UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests/test_action_phase0_common.py acs-jepa-cli/tests/test_action_schema_residuals.py -q`

Observed: **25 passed**, with three third-party deprecation warnings.

## Final local verification

- Adjacent statistics/restoration tests: **17 passed**, three third-party deprecation warnings.
- Core suite: exit 0, all tests passed (`[100%]`), pre-existing third-party warnings only.
- CLI suite excluding the preregistered tuning-config exception: exit 0, all selected tests passed (`[100%]`), pre-existing third-party warnings only.
- Ruff: `All checks passed!`
- `compileall`: exit 0.
- `git diff --check`: exit 0.
- HEAD remained `94604ab5cfe73038f35f62ba160da215a47dd090`; no commit was created.
- Official root `/opt/data/workspace/acs-jepa-runs/smoke/action_auxiliary_seed0/updated_phase0` remained absent.

Two complete baseline CPU runs were executed only under temporary root
`/tmp/acs-jepa-stage0a-repeat-rS9xmK` (not the official root). The exact repeat check returned:

`{'summary_projection_equal': True, 'details_byte_equal': True, 'population': 174780, 'population_sha256': 'c6b056d8a976a77c994338aeceedcc519a7506d770e6921d08d00da4753e97d2', 'deterministic_algorithms': True}`

## Official Stage 0A evidence execution

After the independent software checks above passed, the four reviewed CUDA commands were executed in order under the official immutable root. All exited 0.

- Baseline run1/run2: 174,780 rows, 44 groups, 604 manifest records, 7 schemas, 44 nearest-wrong and 44 nearest-invalid rows.
- Phase 2 run1/run2: the same reconciled counts and population identity.
- Full-population identity in all four summaries: 24,455,400 bytes, SHA-256 `c6b056d8a976a77c994338aeceedcc519a7506d770e6921d08d00da4753e97d2`.
- Baseline repeat: summary decision projection equal; `details.json` byte-identical (44 records; SHA-256 `40a432931f7ba40642e90ce535770e5665b485492bc1de8c14a2e7ba107f8cdb`).
- Phase 2 repeat: summary decision projection equal; `details.json` byte-identical (44 records; SHA-256 `e74534281a136f31c01b5d6691696f3543b4e64723a832fe519b96034fb1912a`).
- Root identity SHA-256: `b26160cb47a8294664d209af04d3f69fdf24f254aa8899bb89606f3e5d808a48`.

Compact Stage 0A metrics:

- Baseline: state/schema residual effective rank `6.371300938481334`; raw within-schema fraction `1.4071546829893256e-05`; nearest-invalid unit-residual median `0.12811160511072886`.
- Phase 2: state/schema residual effective rank `10.334873163207176`; raw within-schema fraction `3.694867485187534e-06`; nearest-invalid unit-residual median `0.25239345211088804`.

These are diagnostic outputs only. No Stage 0B branch decision or training intervention is authorized by them.

## Implementation Review 1 corrections — behavioral RED/GREEN

Independent review `deleg_bf9196c7` returned FAIL and did not authorize commit. It found that the bootstrap REDs did not demonstrate vertical behavior, the exact 174,780-row enumeration identity lacked an end-to-end regression, and the no-simulator regression did not cover the extraction CLI’s transitive helper dependency.

Two focused behavioral tests were added before correction and executed together:

```text
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_phase0_common.py::test_actual_fixed_sources_enumerate_exact_population_identity \
  acs-jepa-cli/tests/test_action_schema_residuals.py::test_schema_residual_extraction_has_no_simulator_or_oracle_dependency -q
```

Observed RED: exit 1, exactly 2 failed. The population regression failed because `enumerate_same_schema_population` did not exist. The dependency regression failed because the extraction CLI imported `action_diag_common`, whose module exposes live simulator replay/oracle helpers.

Minimal GREEN:

- Added a pure `enumerate_same_schema_population` adapter and changed extraction to consume its canonical buckets/identity rows.
- Added simulator-free checkpoint loading, strict restoration, split selection, state encoding, and device resolution to `action_phase0_common.py`.
- Removed `action_diag_common` from the Stage 0A extraction import graph.
- The behavioral regression independently loads the fixed corpus and candidate manifest, recovers 44 sources, enumerates 174,780 actions, and asserts 24,455,400 canonical bytes plus SHA-256 `c6b056d8a976a77c994338aeceedcc519a7506d770e6921d08d00da4753e97d2`.
- The dependency regression parses both Stage 0A modules and rejects simulator, oracle, replay, or `action_diag_common` dependencies.

Observed GREEN: 2 passed.

The pre-correction official root was preserved as `updated_phase0-review1-rejected`; it was not reused as accepted evidence. After the corrected focused suite and static checks passed, the clean official root was recreated through the literal baseline run1 command and all four CUDA commands were rerun. Corrected outputs are projection-identical to the preserved outputs; both `details.json` pairs are byte-identical across repeats and byte-identical to the preserved results. Corrected run1 summary SHA-256 values are baseline `f428905cacf2fd809a6f5504be8b3a7f525893f30f4ec32daf1e69d148ce1843` and Phase 2 `031da2b75aad2e843c41c890b9e33733dff2c01f5622588190881386b445c49b`.

Post-correction verification:

- Focused/adjacent Stage 0A suite: 44 passed.
- Full core suite: 296 passed.
- Scoped CLI suite with the established tuning-config exclusion: 135 passed.
- Ruff, `compileall`, and `git diff --check`: passed.

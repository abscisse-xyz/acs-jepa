# Stage 0B strict vertical RED/GREEN record

Governing plan: `.hermes/plans/2026-07-22_103957-updated-phase0-falsification.md` §§3, 5, 8, 9
Plan-review authorization: Review 3 `PASS` (`deleg_8d8faf37`)
Scope: implementation only; no official evidence, Stage 0C/D, implementation review, commit, or push.

This record distinguishes behavioral RED from bootstrap failures, immediate-pass coverage, and failures caused by a test assertion rather than missing production behavior.

## Prior live tracer sequence (`deleg_1c3362a4`)

Source transcript: `/opt/data/cache/delegation/live/deleg_1c3362a4/task-0.log`.

1. Bootstrap import test failed because `script/diagnose_action_applicability_recoverability.py` did not exist, then passed after the CLI skeleton was created. This was a bootstrap failure, not claimed as behavioral RED.
2. Exact A–E schema/composition test failed with missing `feature_schemas`, then passed after the implementation. The hand-derived raw symbolic fixture passed immediately after that implementation; it is not claimed as a separate RED.
3. Static-source mismatch test failed with missing `validate_static_fact_match`, then passed after the helper was added.
4. Train-only population preprocessing test failed with missing preprocessing helpers, then passed after one expected-value correction and implementation.
5. Pinned split/control-permutation test failed with missing `split_manifest`, then passed.
6. Threshold/tie-aware metrics/calibration/distribution test failed with missing metric helpers, then passed; Phase 2G AUROC/AP helpers were moved to the simulator-free common module.
7. Fifteen-fit ordering/RNG/reconstruction test failed with missing `fit_all_probes`, then passed.
8. Root/checkpoint/destination binding test failed with missing binding constants/helper, then passed.
9. Global-threshold/control-label report semantics test failed with missing `probe_report`, then passed.
10. Focused file then reported `10 passed`.
11. The first ad-hoc smoke bootstrap failed because the script directory was absent from `sys.path`; the corrected invocation completed. The prior agent used shell cleanup in those ad-hoc attempts; no such cleanup command was used in this continuation.

## Continuation behavioral tracers

### Exact predicate vocabulary and argument types

RED:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py::test_raw_symbolic_rejects_any_predicate_vocabulary_or_arg_type_drift -q
```

Observed: `2 failed`; both changed `starting` argument types and an extra predicate were accepted (`Failed: DID NOT RAISE`).

GREEN: pinned the exact ten `(predicate, arg_types)` tuples and compared the complete sorted live map. The same command then reported `2 passed`.

### Static validation is on the extraction path

RED:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py::test_collect_features_invokes_static_source_vs_include_static_graph_validation -q
```

Observed: failed before the sentinel validator because `_collect_features` proceeded directly to `ActionDecodingSpace.from_parsed_problem`; this proved the static validator was unused.

GREEN: `_collect_features` now materializes the exact atom tuple passed to the `include_static=True` encoder, validates static source atoms against that graph input, and then encodes that validated input. The same command reported `1 passed`.

### Immediate-pass and test-correction limitations

- Label/category/future/split metadata mutation coverage passed immediately. These values are not accepted by `raw_symbolic_features` or `compose_feature_sets`; no production change was made and this is not claimed as RED.
- Fresh-runtime project-module import coverage initially failed only because the test matched third-party `antlr4.*ATNSimulator` and `torch._dynamo.replay_record`. After narrowing the assertion to project module prefixes, it passed immediately with no prohibited simulator/replay/oracle/applicable-actions module loaded. The false positive is not claimed as behavioral RED.
- The real two-invocation artifact test initially failed because it incorrectly asserted insertion order after parsing canonical `sort_keys=True` JSON. After changing that map assertion to exact key membership, it passed. This was a test defect, not behavioral RED.
- Full literal 217-name comparison, leakage mutations, recursive five-artifact keys/counts/order, 604 manifest-ordered details, and all-15-model/604-logit reconstruction passed when added against the completed behavior; these are regression coverage, not claimed as test-first RED.

## Real temporary-root repeat smoke

The pytest smoke uses only `tmp_path`, creates a valid temporary `updated_phase0/root_identity.json`, and invokes the real baseline CLI into literal `run1` and `run2` with the fixed 200-step contract. It verifies:

- exact inventory: `summary.json`, `details.json`, `feature_schema.json`, `split_manifest.json`, `probe_states.json`;
- summary equality after deleting only the six governed pointers;
- byte equality of all four non-summary siblings;
- recursive exact keys, five feature sets, 604 ordered details, and 15 model states;
- reconstruction of all 15 models against all 604 serialized logits at `atol=1e-7, rtol=0`;
- refusal of an existing destination and a changed root marker.

A separate manual temporary-root repeat produced byte-identical siblings and summary projection. Observed sibling identities were:

- `details.json`: 521047 bytes, SHA-256 `e758de0d72ba8f5d3276a8f2a51c839de76932d944c8bb59d38e29373734b02e`;
- `feature_schema.json`: 40989 bytes, SHA-256 `1690a4fe469318b1e1cf282ad993e9da8f77b84bf535d660df506c433c9a0a7a`;
- `split_manifest.json`: 455 bytes, SHA-256 `5397fc5e7820c9fdee3eb38c05278a3b680fb5ca8460d0bbe588ffa7ff22815c`;
- `probe_states.json`: 3973139 bytes, SHA-256 `7ba32d0552dcb1b04274915d1ee3fd986c87ee207df24c0cc00f3388dd670c17`.

These are temporary pre-evidence smoke outputs only; they were not written under the official root.

## Final pre-evidence verification

Focused Stage 0B after formatting:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py -q
```

Outcome: `16 passed`.

Adjacent accepted Stage 0A:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_phase0_common.py \
  acs-jepa-cli/tests/test_action_schema_residuals.py -q
```

Outcome: `27 passed`.

Adjacent Phase 2G diagnostics:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_supervised_probes.py -q
```

Outcome: `19 passed`.

Full governed suites:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-core pytest packages/acs-jepa-core/tests -q
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests -q \
  -k 'not tuning_configs_load_and_keep_required_defaults'
```

Outcomes: core `296 passed`; CLI `151 passed`.

Static checks:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --dev ruff check \
  script/action_phase0_common.py \
  script/diagnose_action_supervised_probes.py \
  script/diagnose_action_applicability_recoverability.py \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py
python3 -m compileall -q \
  script/action_phase0_common.py \
  script/diagnose_action_supervised_probes.py \
  script/diagnose_action_applicability_recoverability.py \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py
git diff --check
```

Outcomes: Ruff `All checks passed!`; compileall exit 0; `git diff --check` exit 0.

Warnings were limited to existing dependency deprecations (`torch_geometric`, `torch.jit`, `pkg_resources`, typing internals, and one MLflow filesystem-backend warning in the full CLI suite).

Final guard before evidence: `/opt/data/workspace/acs-jepa-runs/smoke/action_auxiliary_seed0/updated_phase0/recoverability` was absent. No Stage 0B implementation review, commit, or push had been performed.

## Official Stage 0B evidence

After the complete pre-evidence verification above, the four preregistered §5.4 CPU commands were executed unchanged for baseline `run1`/`run2` and Phase 2 `run1`/`run2`. All exited 0. The official root marker created by accepted Stage 0A was validated and no destination was overwritten.

Repeat verification independently observed for both checkpoints:

- projected `summary.json` equality after deleting only the six governed pointers: `True`;
- `details.json`, `feature_schema.json`, `split_manifest.json`, and `probe_states.json`: byte-identical between repeats.

Official non-summary identities:

- baseline `details.json`: 521047 bytes, `e758de0d72ba8f5d3276a8f2a51c839de76932d944c8bb59d38e29373734b02e`;
- baseline `feature_schema.json`: 40989 bytes, `1690a4fe469318b1e1cf282ad993e9da8f77b84bf535d660df506c433c9a0a7a`;
- baseline `split_manifest.json`: 455 bytes, `5397fc5e7820c9fdee3eb38c05278a3b680fb5ca8460d0bbe588ffa7ff22815c`;
- baseline `probe_states.json`: 3973139 bytes, `7ba32d0552dcb1b04274915d1ee3fd986c87ee207df24c0cc00f3388dd670c17`;
- Phase 2 `details.json`: 521068 bytes, `655d87ea604831dddfe626740d37b17bdda56e981e6f6361f4bb174d7de0fa92`;
- Phase 2 `feature_schema.json`: same identity as baseline;
- Phase 2 `split_manifest.json`: same identity as baseline;
- Phase 2 `probe_states.json`: 3972736 bytes, `f5f2bc59e78531cec993b6f2adfb01886ac0a11cec156f964e028eb6a7527c3b`.

The fixed MLP gate classified baseline and Phase 2 `latent_separable`, `raw_separable`, and `hybrid_separable` as true, with controls below 0.70. Phase 2 decision-bearing values were:

- C selected graph/action: AUROC `0.8446078431372549`, AP `0.37933323482921644`, role-swap median `0.15346449613571167`, one-substitution median `0.5543402433395386`, control AUROC `0.5651960784313725`;
- D raw symbolic: AUROC `0.9936274509803922`, AP `0.9358169608169609`, role-swap median `5.373201746493578`, one-substitution median `2.6721105873584747`, control AUROC `0.4843137254901961`;
- E hybrid: AUROC `0.9715686274509804`, AP `0.8437950992362758`, role-swap median `5.10102704167366`, one-substitution median `4.133744776248932`, control AUROC `0.5852941176470589`.

These are Stage 0B diagnostic classifications only. No branch decision, Stage 0C implementation, commit, or push is authorized without independent implementation/evidence review PASS.

## Review1 FAIL correction (`deleg_74eba911`)

Independent implementation/evidence Review1 failed on three blockers: tautological static-fact validation, absent optimizer-step mutation coverage, and metadata/prefix causality tests that did not exercise the real extraction boundary. The accepted official evidence above was intentionally left untouched; this correction returns Stage 0B only to pre-evidence green so the parent can archive and rerun evidence.

### Actual graph reconstruction and extraction-boundary causality — behavioral RED/GREEN

RED command:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py::test_actual_graph_static_reconstruction_detects_filtering_and_encodes_same_validated_data \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py::test_actual_source_extraction_boundary_has_exact_metadata_and_prefix_causality -q
```

Observed: `2 failed`. The graph mutation failed because the production module had no independently patchable `build_state_graph` path, and both tests failed because `_extract_source_features` did not exist. These were the expected missing production boundaries, not assertion or bootstrap defects.

GREEN: added an actual per-source extraction adapter used by `_collect_features`. It builds one `include_static=True` `Data`, independently reconstructs every `GroundAtom` from object IDs, atom-node predicate IDs/arities, and exact role/direction edge pairs, validates the recorded static subset against that reconstruction, and passes that same validated `Data` object to `bundle.jepa.encode`. The filtering mutation (`include_static=False`) now raises `recorded static facts do not match include_static graph input` before encode, while the correct graph is encoded by identity. A faithful actual `p166:0` source/record mutation now proves label/category plus inert future/split metadata leave A–E bytewise tensor-equal; removing the deterministic dynamic source atom `clear(junction3-2)` leaves A and all action/selected/mask partitions unchanged, changes the graph partitions, changes exactly `fact:clear(1)` in D, and changes exactly the corresponding graph/raw coordinates in E. The same command then reported `2 passed`.

### Full-batch optimizer-step mutation — immediate-pass regression

The strengthened existing fit test spies `torch.optim.Adam.step`, retains all optimizer instances to avoid object-ID reuse, and asserts exactly 15 distinct optimizers, exactly `epochs` calls for each, and exactly `15 * epochs` calls overall while preserving A–E/model order and reconstruction checks.

Command:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py::test_fit_inventory_order_steps_rng_reset_and_all_fifteen_states_reconstruct -q
```

Observed: `1 passed` immediately against the existing fitting loop. This is regression coverage, not claimed as RED; no production fitting change was needed.

### Corrected focused result

After correcting the superseded sentinel test to assert `_collect_features` invokes the validated per-source adapter, the complete focused command reported `18 passed`.

### Corrected pre-evidence verification

- focused Stage 0B: `18 passed`;
- adjacent accepted Stage 0A: `27 passed`;
- adjacent Phase 2G: `19 passed`;
- full core: `296 passed`;
- scoped CLI: `153 passed` (the prior 151 plus the two new behavioral tests);
- Ruff: `All checks passed!`;
- `compileall`: exit 0;
- `git diff --check`: exit 0.

Warnings remained limited to the previously observed dependency deprecations and MLflow filesystem-backend warning. No official Stage 0B evidence command was rerun during the correction itself and no evidence artifact, commit, push, or later-stage file was modified.

### Corrected official evidence rerun

After parent verification of the 18 focused tests, Ruff, `compileall`, and `git diff --check`, the Review1 evidence was first preserved and later relocated outside the accepted root to:

`/opt/data/workspace/acs-jepa-runs/smoke/action_auxiliary_seed0/updated_phase0-stage0b-review1-rejected`

The accepted destination was made absent, then all four exact §5.4 CPU commands were rerun into a newly created `recoverability` root. All commands exited 0. Exact-six-pointer summary projections matched between repeats for baseline and Phase 2. Every non-summary sibling was byte-identical between repeats. The corrected evidence also matched the preserved Review1 evidence under the same exact-six-pointer/byte comparisons, confirming the correction strengthened validation without changing extraction or fitted results. Official identities and all decision metrics remain those recorded above.

## Review2 FAIL correction

Independent Review2 failed the corrected Stage 0B evidence on two remaining implementation blockers:

1. `reconstruct_graph_atoms` ignored unmatched object-to-atom edges, so it did not enforce the exact complete bidirectional edge multiset.
2. The optimizer regression counted optimizer instances and steps but did not inspect the population supplied to every BCE/update, so a one-row `x[:1]`/`targets[:1]` training mutation could retain all asserted counts.

The accepted and rejected official evidence was intentionally left untouched. This correction returns Stage 0B only to pre-evidence green.

### Exact bidirectional state-graph edge multiset — behavioral RED/GREEN

The adversarial regression mutates the actual `p166:0` PyG `Data` with an unmatched reverse-only edge, unmatched forward-only edge, duplicate bidirectional pair, invalid direction, atom-to-atom connectivity, object-to-object connectivity, and arity/role mismatch. Before every mutation it independently verifies correct reconstruction of all `128/128` source atoms.

RED command:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py::test_graph_atom_reconstruction_requires_exact_bidirectional_edge_multiset -q
```

Observed before the production correction: `3 failed, 3 passed`. In particular, `unmatched_reverse_only`, `invalid_direction`, and `invalid_object_atom_connectivity` each failed with `Failed: DID NOT RAISE`; this directly proved the reverse-only acceptance blocker.

GREEN: `reconstruct_graph_atoms` now reconstructs atom arguments from valid forward role edges, derives the complete expected forward-plus-reverse edge `Counter`, and compares it with a `Counter` containing every actual `edge_index`/`edge_attr` entry. Any extra, missing, duplicate, invalid-direction, invalid-connectivity, or role/arity-mismatched edge is rejected.

The final expanded command then reported `7 passed`. Correct `p166:0` reconstruction remained exactly `128/128`; the focused extraction regression continued to prove that the exact validated `Data` identity reaches `bundle.jepa.encode`.

### Full-train-population BCE/update regression — immediate pass plus mutation proof

The existing fit regression now spies `torch.nn.functional.binary_cross_entropy_with_logits` only around `fit_all_probes` and records both logits and target shapes for every call. With train population `N=4` and `epochs=2`, it requires exactly `15 * 2` calls and exact population shapes `((4,), (4,))` on every call, in addition to 15 distinct Adam optimizers, two steps per optimizer, 30 total steps, fixed fit order/RNG resets, and all-15 state reconstruction.

Unmutated command:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py::test_fit_inventory_order_steps_rng_reset_and_all_fifteen_states_reconstruct -q
```

Observed immediately against the existing production fitting loop: `1 passed`. This is an immediate-pass regression, not claimed as behavioral RED; no production fitting change was needed.

Mutation proof: temporarily changing the BCE call to `model(x[:1])` and `targets[:1]`, then running the exact command above, reported `1 failed` at the population assertion: observed `((1,), (1,))` versus required `((4,), (4,))`. After restoring the unmutated full-batch line, the exact command again reported `1 passed`.

### Review2 corrected pre-evidence verification

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py -q
```

Outcome: focused Stage 0B `25 passed`.

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_phase0_common.py \
  acs-jepa-cli/tests/test_action_schema_residuals.py -q
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_supervised_probes.py -q
```

Outcomes: adjacent Stage 0A `27 passed`; adjacent Phase 2G `19 passed`.

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-core pytest packages/acs-jepa-core/tests -q
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest acs-jepa-cli/tests -q \
  -k 'not tuning_configs_load_and_keep_required_defaults'
```

Outcomes: full core `296 passed`; scoped CLI `160 passed`.

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --dev ruff check \
  script/action_phase0_common.py \
  script/diagnose_action_supervised_probes.py \
  script/diagnose_action_applicability_recoverability.py \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py
python3 -m compileall -q \
  script/action_phase0_common.py \
  script/diagnose_action_supervised_probes.py \
  script/diagnose_action_applicability_recoverability.py \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py
git diff --check
```

Outcomes: Ruff `All checks passed!`; `compileall` exit 0; `git diff --check` exit 0. Warnings remained limited to the previously observed dependency deprecations and MLflow filesystem-backend warning. No official or rejected evidence artifact, commit, push, or later-stage file was modified during the correction itself.

### Review2-corrected official evidence rerun

Parent reverified the 25 focused tests and static checks, then moved the prior accepted evidence outside the protocol-owned root to:

`/opt/data/workspace/acs-jepa-runs/smoke/action_auxiliary_seed0/updated_phase0-stage0b-review2-rejected`

All four exact §5.4 commands were rerun into a clean `updated_phase0/recoverability` destination and exited 0. For baseline and Phase 2, exact-six-pointer summary projections matched between run1/run2 and every non-summary sibling was byte-identical. The accepted outputs also matched both preserved Review1 and Review2 evidence under exact-six-pointer/byte comparison, so strict edge validation changed no valid extraction, metrics, fit, verdict, or artifact identity. The accepted `updated_phase0` root contains no rejected-evidence sibling.

## Review3 FAIL correction

Independent Review3 failed the Stage 0B implementation because the full-train fit regression checked only `(4,)` logits/target shapes. A same-shape mutant that dropped canonical row 1 and duplicated row 0 for both model input and BCE target survived, so exact full-population values and canonical row ownership were not enforced. No production, evidence, commit, or later-stage file was changed for this correction.

### Exact full-train values and canonical A–E/model/epoch order — immediate-pass regression with mutation proof

The strengthened `test_fit_inventory_order_steps_rng_reset_and_all_fifteen_states_reconstruct` now gives A–E five separately derived, non-affine canonical row patterns; independently computes each expected float64 population-standardized tensor; instruments every created model with a forward pre-hook; records every BCE target; and uses `torch.equal` on all exact `15 * 2` forward inputs and targets in A–E, `linear`/`mlp`/`control_mlp`, epoch order. Linear and MLP calls require the original label vector; control MLP calls require the single control vector. Existing exact optimizer-instance, per-optimizer step, total-step, RNG-reset, fit-inventory, and all-fifteen-state reconstruction assertions remain.

Unmutated command:

```bash
UV_CACHE_DIR=/opt/data/workspace/.uv-cache uv run --package acs-jepa-cli pytest \
  acs-jepa-cli/tests/test_action_applicability_recoverability.py::test_fit_inventory_order_steps_rng_reset_and_all_fifteen_states_reconstruct -q
```

Observed immediately against the unmodified fitting implementation: `1 passed`. This is regression coverage, not behavioral RED; no production fitting change was needed.

Mutation proof is test-local and automatically restored by `monkeypatch.context()`: a same-shape mutant replaces row 1 with a duplicate of row 0 in both the tensor reaching every model and the BCE target. The exact forward assertion fails with `forward call 0 changed value or canonical row order`; independently, the exact target assertion fails with `BCE call 0 changed target value or row order`. Both failures are required by `pytest.raises`, and the exact unmutated command above passes after restoration. No mutant remains in production or test source.

### Review3-corrected pre-review verification

- exact fit regression: `1 passed` after test-local mutant restoration;
- focused Stage 0B: `25 passed`;
- adjacent accepted Stage 0A: `27 passed`;
- adjacent Phase 2G: `19 passed`;
- full core: `296 passed`;
- scoped full CLI: `160 passed` with only `tuning_configs_load_and_keep_required_defaults` excluded as in prior governed runs;
- Ruff: `All checks passed!`;
- `compileall`: exit 0;
- tracked and untracked-test whitespace/diff checks: exit 0.

Warnings remained limited to the previously observed dependency deprecations and MLflow filesystem-backend warning. The final diff was inspected. This correction modified only this RED/GREEN record and the Stage 0B regression test; it made no production, official/archived evidence, commit, push, or later-stage change.

# Updated Phase 0 Falsification — Review Record

Plan: `.hermes/plans/2026-07-22_103957-updated-phase0-falsification.md`
Governing spec: `script/ACTION_LATENT_UPDATED_SPEC.md`

## Plan Review 1 — FAIL

- Date: 2026-07-22
- Delegation: `deleg_28d4f09c`
- Authorization: implementation not authorized.

Concrete blockers:

1. Manifest group ranges contradicted the pinned bytes.
2. Accepted Phase 2G split arrays/hash were not pinned and could not be regenerated from manifest encounter order.
3. Raw features assumed unavailable parsed action preconditions.
4. Raw feature dimensions/order/static/constant semantics were ambiguous.
5. Stage 0A similarity margins lacked operational anchors and were tautological under the apparent interpretation.
6. Stage 0D could not consume accepted Stage 0B probes from the planned artifacts.
7. Branch B depended on non-computable off-manifold language and an unspecified sixth scorer.
8. Branch A/B/C predicates did not exactly encode named evidence.
9. Exact summary/supporting/evidence schemas and repeat scope were absent.

## Corrections before Plan Review 2

- Replaced range shorthand with all 44 literal groups (`p166:0..22`, `p192:0..20`).
- Pinned literal 33 train/11 eval arrays, 453/151 record counts, 455 canonical bytes, and SHA-256 `5397fc5e7820c9fdee3eb38c05278a3b680fb5ca8460d0bbe588ffa7ff22815c`.
- Replaced unavailable precondition parsing with an exact 217-dimensional CityCar raw fact/binding vector using current `ParsedProblem`/`GroundAtom` APIs; excluded problem-local IDs and simulator-derived features.
- Expanded Stage 0A to the exact 174,780 full same-schema population, added global-schema and state/schema residuals, and replaced undefined margins with exact nearest-wrong/nearest-invalid distances.
- Required Stage 0B to serialize canonical train/eval logits and deterministic probe states; Stage 0D restores/checks them without refitting applicability probes.
- Defined five Stage 0D scorers exactly; removed the post-hoc stacker/sixth ablation. Teacher-forced transition scoring is explicitly non-deployable.
- Defined nine exact decision booleans and deterministic seven-clause precedence for continuation/FIX/A/B/C/D outcomes.
- Added strict schemas for every summary, details/supporting file, assessment, and evidence manifest; only summaries use the six-pointer projection while sibling JSON must be byte-identical.
- Pinned baseline/Phase 2 config hashes and assessor inputs.
- Incorporated read-only related-work review (`deleg_bdbbda80`): pooled/per-schema RankMe evidence, collapse caveat, linear/nonlinear probe controls, AP/prevalence under imbalance, separation of calibration from ranking, two-part transition equivalence, and discrete candidate-ranking precedent. No project threshold was changed from observed Phase 0 results (none exist).

Implementation remains unauthorized pending Plan Review 2 PASS.

## Plan Review 2 — FAIL

- Date: 2026-07-22
- Delegation: `deleg_750b3860`
- Authorization: implementation not authorized.

Verified Review 1 corrections and live identities, but found six remaining blockers: undefined residual decision formulas/paths; omitted control-AUROC in branch separability booleans; ambiguous role/object scorer denominator/training/state; Phase 2-only ranking despite a two-checkpoint comparison contract; incomplete recursive JSON schemas; and ambiguous shared-root refusal ownership.

## Corrections before Plan Review 3

- Defined raw total/between/within variance with population denominators, reconstruction tolerance, exact JSON paths, and state/schema residual covariance/effective-rank formulas. `representation_ok` now names exact baseline/Phase 2 paths.
- Added the corresponding control-probe AUROC gate and exact feature/model paths to all three separability booleans.
- Pinned `RoleObjectProbe` architecture, all-manifest-active-role training rows, all-real-object mask (never type mask), sorted bank targets, full-batch CE, complete-action mean log-probability, fitted-model return compatibility, and canonical role-probe state.
- Added baseline candidate-ranking runs, artifacts, assessor inputs, repeat comparison, and baseline/Phase 2 compact ranking maps; branch predicates remain explicitly Phase 2.
- Completed nested settings values, seven-schema/three-category maps, nullability, root/support files, repeatability, verdict literals, precedence predicate literals, compact metrics, and exact sibling inventories; canonical probe states now include train-only preprocessing arrays/indices so Stage 0D can reproduce logits without refitting.
- Assigned clean-root creation and `root_identity.json` to the literal Stage 0A baseline run1 command; every later command validates the marker and refuses only its own destination. Confirmed the official root is currently absent.

Implementation remains unauthorized pending Plan Review 3 PASS.

## Plan Review 3 — FAIL

- Date: 2026-07-22
- Delegation: `deleg_1c0b7436`
- Authorization: Stage 0A implementation not authorized.

The reviewer found four remaining blockers: incomplete recursive architecture/dtype/optimizer/policy/schema-version literals; ranking artifacts not cryptographically bound to all five consumed Stage 0B files or independently reconciled by the assessor; a prose promise of deltas absent from the exact assessment schema; and ambiguous per-schema covariance normalization.

## Corrections before Plan Review 4

- Fixed pooled covariance to divide by `N=174780` and each per-schema covariance by its own `n_s`.
- Pinned all diagnostic/supporting/assessment/evidence schema-version literals, applicability and role-probe architecture strings, canonical `torch.float32`, exact Adam configuration, role mask policy, row order, and evidence role vocabulary.
- Added five canonical `recoverability_inputs` identities to every matching ranking summary. Both ranking repeats bind the same accepted matching-checkpoint Stage 0B run1 inputs, preserving repeat-summary equality. Stage 0D copies the three imported score vectors exactly; the assessor validates those paths/hashes, reconstructs all ten probes with preprocessing, reproduces all 604 logits, and reconciles all 151 imported scores.
- Replaced the unsupported delta promise with exact side-by-side baseline and Phase 2 metric maps.

Implementation remains unauthorized pending Plan Review 4 PASS.

## Plan Review 4 — PASS

- Date: 2026-07-22
- Delegation: `deleg_3041b50e`
- Authorization: Stage 0A implementation only.
- Scope: Stages 0B–0D remain blocked behind their own implementation-plan/review gates.

The reviewer independently verified all Review 3 corrections, exact input hashes, current APIs, the 16-key summary contract, recursive schemas, repeat projections, command paths, root ownership, and the absence of the official output root. No blocking contradiction or materially undefined Stage 0A behavior remained. Stage 0A may proceed under strict RED/GREEN; no later stage is authorized by this verdict.

## Stage 0A Implementation/Evidence Review 1 — FAIL

- Date: 2026-07-22
- Delegation: `deleg_bf9196c7`
- Authorization: signed commit not authorized.

The reviewer accepted the artifacts, formulas, repeatability, suites, and static checks, but blocked commit because the initial RED record was bootstrap-only, the actual 174,780-row population lacked a regression test, and the no-simulator test did not cover the extraction dependency graph. Corrections and their behavioral RED/GREEN evidence are recorded in `.hermes/plans/2026-07-22_103957-stage0a-red-green.md`.

## Stage 0A Implementation/Evidence Review 2 — PASS

- Date: 2026-07-22
- Delegation: `deleg_800c1971`
- Authorization: verified SSH-signed Stage 0A commit authorized; Stages 0B–0D remain unauthorized.

The reviewer independently reproduced the exact fixed population identity, verified the correction RED/GREEN timestamps and outputs, proved the tested enumeration adapter is consumed by extraction, found no Stage 0A simulator/oracle/replay dependency, established copied helper equivalence against the prior diagnostic helpers, revalidated corrected official evidence and repeatability, and passed focused, core, scoped CLI, Ruff, `compileall`, and diff checks.

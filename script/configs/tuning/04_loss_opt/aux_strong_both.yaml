model:
  loss:
    similarity_coeff: 0.25
    inverse_dynamics_coeff: 0.5
tracking:
  experiment_name: acs-jepa-tuning-v1
  run_name: stage4_aux_strong_both
  tags:
    stage: loss_opt
    variant: aux_strong_both

model:
  goal_head:
    kind: conditional_sampler
tracking:
  experiment_name: acs-jepa-tuning-v1
  run_name: stage1_goal_conditional_sampler
  tags:
    stage: composition
    variant: goal_conditional_sampler
